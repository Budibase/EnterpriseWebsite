<hr class="divider" />

<style>
  .divider {
    width: calc(100% - 2 * var(--site-content-inset));
    max-width: calc(var(--site-container-max) - 2 * var(--site-content-inset));
    margin: 0 auto clamp(2rem, 4vw, 3rem);
    border: 0;
    border-top: 1px solid var(--bb-border-base);
  }
</style>
