---
const { frontmatter } = Astro.props;
import MainLayout from './MainLayout.astro';

---
<MainLayout>
    <div class="max-w-xl mx-auto">
    <h1 class="mb-4 mt-8">{frontmatter.title}</h1>
    <p>Written by {frontmatter.author}</p>
    <slot />
    </div>
</MainLayout>
